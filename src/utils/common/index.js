export const DATE_TODAY = () => {
    return new Date().toISOString().slice(0, 10).split('-').join('-')
}

export const parseDate = (date) => {
    return new Date().toISOString().slice(0, 10).split('-').join('-')
}

